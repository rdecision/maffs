#include "../../include/linalg/vector.h"

namespace linalg
{
	template <typename T>
	Vector<T> Vector<T>::operator+(const Vector& a)
	{
		size_t this_size = sizeof(this.data) / size(this.data[0]);
		size_t a_size = sizeof(this.data) / size(this.data[0]);

		return nullptr;
	}

}